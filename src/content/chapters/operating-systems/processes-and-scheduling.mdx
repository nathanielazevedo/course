A process is a running program. It is more than just code — the OS wraps each running program in a container of state:

- A **virtual address space** — the range of memory addresses the process can use.
- **Open file descriptors** — references to files, sockets, and pipes the process has opened.
- **A PID** — a unique process ID assigned by the OS.
- **CPU registers and program counter** — the execution state, saved when the process is not actively running.
- **Metadata** — owner, permissions, parent process, exit status.

### Process States

At any moment a process is in one of three states:

- **Running** — currently executing on a CPU core.
- **Ready** — able to run, but waiting for a CPU core to become available.
- **Blocked** — waiting for something external (I/O, a lock, a timer). Cannot run even if a core is free.

A process moves between these states constantly. Calling `read()` on a file not in the kernel's cache moves the process from running to blocked. When the data arrives from disk, it moves to ready. When the scheduler picks it, it moves back to running.

<figure style={{ margin: "32px 0", textAlign: "center" }}>
  <svg
    viewBox="0 0 720 360"
    width="100%"
    height="auto"
    role="img"
    aria-label="A process state machine with three states: Running at the top, Blocked at the bottom-left, Ready at the bottom-right. Running transitions to Blocked when waiting for I/O; Running and Ready transition both ways with time slices and scheduler decisions; Blocked transitions to Ready when I/O completes."
    style={{ fontFamily: 'ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace' }}
  >
    <defs>
      <marker id="state-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#000" />
      </marker>
    </defs>

    {/* Title */}
    <text x="360" y="22" fontSize="9" letterSpacing="2" fill="#666" textAnchor="middle">PROCESS STATE MACHINE</text>

    {/* Running */}
    <rect x="290" y="50" width="140" height="60" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="360" y="78" fontSize="14" fontWeight="600" textAnchor="middle">Running</text>
    <text x="360" y="95" fontSize="10" fontStyle="italic" fill="#666" textAnchor="middle">on a CPU core</text>

    {/* Blocked */}
    <rect x="90" y="220" width="140" height="60" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="160" y="248" fontSize="14" fontWeight="600" textAnchor="middle">Blocked</text>
    <text x="160" y="265" fontSize="10" fontStyle="italic" fill="#666" textAnchor="middle">waiting on I/O</text>

    {/* Ready */}
    <rect x="490" y="220" width="140" height="60" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="560" y="248" fontSize="14" fontWeight="600" textAnchor="middle">Ready</text>
    <text x="560" y="265" fontSize="10" fontStyle="italic" fill="#666" textAnchor="middle">waiting for a core</text>

    {/* Running → Blocked (straight diagonal) */}
    <line x1="310" y1="110" x2="230" y2="220" stroke="#000" strokeWidth="1" markerEnd="url(#state-arrow)" />
    <text x="222" y="170" fontSize="10" fontStyle="italic" fill="#666" textAnchor="end">waits for I/O</text>

    {/* Running → Ready (curved right) */}
    <path d="M 430 110 Q 520 150 490 220" fill="none" stroke="#000" strokeWidth="1" markerEnd="url(#state-arrow)" />
    <text x="538" y="155" fontSize="10" fontStyle="italic" fill="#666" textAnchor="start">time slice ends</text>

    {/* Ready → Running (curved left) */}
    <path d="M 490 220 Q 405 195 410 115" fill="none" stroke="#000" strokeWidth="1" markerEnd="url(#state-arrow)" />
    <text x="392" y="160" fontSize="10" fontStyle="italic" fill="#666" textAnchor="end">scheduler picks</text>

    {/* Blocked → Ready (horizontal) */}
    <line x1="230" y1="250" x2="490" y2="250" stroke="#000" strokeWidth="1" markerEnd="url(#state-arrow)" />
    <text x="360" y="240" fontSize="10" fontStyle="italic" fill="#666" textAnchor="middle">I/O completes</text>
  </svg>
  <figcaption style={{ fontSize: "0.85rem", color: "#666", marginTop: "8px" }}>
    Note that Blocked → Ready, not Blocked → Running: a process whose I/O completes still has to wait for the scheduler to pick it.
  </figcaption>
</figure>

### Context Switching

When the OS switches from one process to another it performs a *context switch*:

1. Save the current process's CPU registers and program counter to memory.
2. Load the saved state of the next process.
3. Resume execution where that process left off.

A context switch costs on the order of microseconds. It happens thousands of times per second on a busy system.

### The Scheduler

The scheduler decides which ready process runs next. Modern schedulers balance several goals:

- **Fairness** — every process gets a reasonable share of CPU time.
- **Responsiveness** — interactive processes (a shell, a GUI) should feel immediate.
- **Throughput** — batch jobs should complete efficiently.

Linux uses the Completely Fair Scheduler (CFS), which tracks how much CPU time each process has received and always picks the process that has received the least. Higher-priority processes accumulate CPU time more slowly, so they get scheduled more often.

**Preemption** means the OS can interrupt a running process at any time — it does not wait for the process to yield voluntarily. This is why a busy loop in one program does not freeze the rest of the system.

### Creating Processes

On Unix, new processes are created with `fork()`, which copies the current process, and `exec()`, which replaces the copied process's image with a new program. The shell uses exactly this sequence when you run a command: it forks itself, then execs the target binary in the child.
