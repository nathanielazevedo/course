Every process believes it has the entire address space to itself. On a 64-bit system, each process sees an address space of up to 16 exabytes — far more memory than any machine actually has. This illusion is *virtual memory*.

### Pages and Page Tables

Physical RAM is divided into fixed-size chunks called *pages* (typically 4 KB). Virtual address space is divided into pages of the same size. The OS maintains a *page table* for each process: a mapping from virtual page numbers to physical page numbers.

When the CPU accesses a virtual address, the MMU (memory management unit) — a hardware component — walks the page table to find the corresponding physical address. This translation happens on every memory access.

<figure style={{ margin: "32px 0", textAlign: "center" }}>
  <svg
    viewBox="0 0 720 460"
    width="100%"
    height="auto"
    role="img"
    aria-label="Process A and Process B each have a separate virtual address space. Each process's virtual pages map to physical pages in RAM via per-process page tables. One physical page (P2) is shared by both processes; another virtual page is swapped to disk; two physical pages are free."
    style={{ fontFamily: 'ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace' }}
  >
    <defs>
      <marker id="vm-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="#666" />
      </marker>
    </defs>

    {/* Title */}
    <text x="360" y="22" fontSize="9" letterSpacing="2" fill="#666" textAnchor="middle">VIRTUAL TO PHYSICAL MEMORY MAPPING</text>

    {/* PROCESS A */}
    <text x="140" y="46" fontSize="10" letterSpacing="2" fontWeight="600" textAnchor="middle">PROCESS A</text>
    <rect x="60" y="60" width="160" height="50" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="140" y="89" fontSize="14" fontWeight="600" textAnchor="middle">V0</text>
    <rect x="60" y="114" width="160" height="50" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="140" y="143" fontSize="14" fontWeight="600" textAnchor="middle">V1</text>
    <rect x="60" y="168" width="160" height="50" fill="#fafafa" stroke="#bbb" strokeWidth="1" strokeDasharray="3 3" />
    <text x="140" y="190" fontSize="12" fill="#888" textAnchor="middle">V2</text>
    <text x="140" y="207" fontSize="9" fontStyle="italic" fill="#888" textAnchor="middle">(swapped to disk)</text>

    {/* PROCESS B */}
    <text x="580" y="46" fontSize="10" letterSpacing="2" fontWeight="600" textAnchor="middle">PROCESS B</text>
    <rect x="500" y="60" width="160" height="50" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="580" y="89" fontSize="14" fontWeight="600" textAnchor="middle">V0</text>
    <rect x="500" y="114" width="160" height="50" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="580" y="143" fontSize="14" fontWeight="600" textAnchor="middle">V1</text>
    <rect x="500" y="168" width="160" height="50" fill="#fff" stroke="#000" strokeWidth="1.5" />
    <text x="580" y="197" fontSize="14" fontWeight="600" textAnchor="middle">V2</text>

    {/* Page table annotation */}
    <text x="360" y="252" fontSize="11" fontStyle="italic" fill="#888" textAnchor="middle">(via per-process page tables)</text>

    {/* Mapping arrows */}
    <line x1="140" y1="110" x2="320" y2="310" stroke="#666" strokeWidth="1" markerEnd="url(#vm-arrow)" />
    <line x1="140" y1="164" x2="160" y2="310" stroke="#666" strokeWidth="1" markerEnd="url(#vm-arrow)" />
    <line x1="580" y1="110" x2="480" y2="310" stroke="#666" strokeWidth="1" markerEnd="url(#vm-arrow)" />
    <line x1="580" y1="164" x2="320" y2="310" stroke="#666" strokeWidth="1" markerEnd="url(#vm-arrow)" />
    <line x1="580" y1="218" x2="560" y2="310" stroke="#666" strokeWidth="1" markerEnd="url(#vm-arrow)" />

    {/* PHYSICAL RAM */}
    <text x="360" y="296" fontSize="10" letterSpacing="2" fontWeight="600" textAnchor="middle">PHYSICAL RAM</text>

    <rect x="120" y="310" width="80" height="80" fill="#f0f0f0" stroke="#000" strokeWidth="1.5" />
    <text x="160" y="354" fontSize="14" fontWeight="600" textAnchor="middle">P0</text>

    <rect x="200" y="310" width="80" height="80" fill="#fff" stroke="#bbb" strokeWidth="1" strokeDasharray="3 3" />
    <text x="240" y="346" fontSize="12" fill="#888" textAnchor="middle">P1</text>
    <text x="240" y="362" fontSize="9" fontStyle="italic" fill="#888" textAnchor="middle">(free)</text>

    <rect x="280" y="310" width="80" height="80" fill="#f0f0f0" stroke="#000" strokeWidth="1.5" />
    <text x="320" y="346" fontSize="14" fontWeight="600" textAnchor="middle">P2</text>
    <text x="320" y="364" fontSize="9" fontStyle="italic" fill="#666" textAnchor="middle">shared</text>

    <rect x="360" y="310" width="80" height="80" fill="#fff" stroke="#bbb" strokeWidth="1" strokeDasharray="3 3" />
    <text x="400" y="346" fontSize="12" fill="#888" textAnchor="middle">P3</text>
    <text x="400" y="362" fontSize="9" fontStyle="italic" fill="#888" textAnchor="middle">(free)</text>

    <rect x="440" y="310" width="80" height="80" fill="#f0f0f0" stroke="#000" strokeWidth="1.5" />
    <text x="480" y="354" fontSize="14" fontWeight="600" textAnchor="middle">P4</text>

    <rect x="520" y="310" width="80" height="80" fill="#f0f0f0" stroke="#000" strokeWidth="1.5" />
    <text x="560" y="354" fontSize="14" fontWeight="600" textAnchor="middle">P5</text>
  </svg>
  <figcaption style={{ fontSize: "0.85rem", color: "#666", marginTop: "8px" }}>
    Each process has its own virtual address space; the page table per process maps virtual pages to physical pages. Some physical pages are shared (P2), some are free (P1, P3), and some virtual pages have been swapped to disk (A.V2).
  </figcaption>
</figure>

### The TLB

Walking the full page table on every access would be slow. The CPU caches recent translations in a small, fast hardware structure called the *TLB* (translation lookaside buffer). A TLB hit costs ~1 cycle; a miss requires a full page table walk.

Context switches flush the TLB on most architectures, which is one reason they are expensive — the first memory accesses after a switch are slower until the TLB warms up again.

### Page Faults

If a process accesses a virtual address with no physical page mapped to it, the CPU raises a *page fault*. The OS handles it:

- **Valid fault** — the page exists but has been swapped to disk. The OS loads it into RAM, updates the page table, and resumes the process transparently.
- **Invalid fault** — the process accessed an address it has no right to. The OS kills the process with a signal. In C, this surfaces as a segmentation fault.

### Benefits of Virtual Memory

- **Isolation** — two processes can use the same virtual addresses without conflict; they have separate page tables pointing to different physical memory.
- **More memory than RAM** — infrequently used pages can be swapped to disk and reloaded on demand, making the total addressable memory larger than physical RAM.
- **Efficient sharing** — read-only pages (like shared library code) can be mapped into multiple processes' address spaces while backed by a single physical page.
- **Memory-mapped files** — a file can be mapped directly into the address space so that reading and writing it becomes ordinary memory accesses, with the OS handling all I/O behind the scenes.
